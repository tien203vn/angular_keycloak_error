import { Route } from '@angular/router';
import { HomeComponent } from '../../web/page/module/home/home.component';

export const routes: Route[] = [
    {
        path: '',
        component: HomeComponent
    }
]